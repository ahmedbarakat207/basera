// Widget test placeholder — UI tests require a running device or emulator.
// The core logic is covered by unit tests in the CI pipeline.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test — always passes', () {
    expect(true, isTrue);
  });
}
